import { NextRequest,NextResponse } from "next/server";import {z} from "zod";import {currentOrganization} from "@/lib/supabase/route";
const product=z.object({name:z.string().min(1),description:z.string().optional(),price:z.coerce.number().nonnegative().optional(),currency:z.string().length(3).default("EUR"),benefits:z.array(z.string()).default([]),features:z.array(z.string()).default([]),conditions:z.string().optional(),guarantee:z.string().optional(),payment_link:z.string().url().optional().or(z.literal("")),faq:z.array(z.object({question:z.string(),answer:z.string()})).default([])});
export async function GET(req:NextRequest){const {db,organizationId}=await currentOrganization(req);if(!db||!organizationId)return NextResponse.json({error:"Non autorisé"},{status:401});const {data,error}=await db.from("products").select("*").eq("organization_id",organizationId).order("created_at",{ascending:false});return error?NextResponse.json({error:error.message},{status:400}):NextResponse.json(data)}
export async function POST(req:NextRequest){const {db,organizationId}=await currentOrganization(req);if(!db||!organizationId)return NextResponse.json({error:"Non autorisé"},{status:401});try{const input=product.parse(await req.json());const {data,error}=await db.from("products").insert({...input,organization_id:organizationId}).select().single();return error?NextResponse.json({error:error.message},{status:400}):NextResponse.json(data,{status:201})}catch(e){return NextResponse.json({error:e instanceof z.ZodError?e.flatten():"Données invalides"},{status:400})}}
